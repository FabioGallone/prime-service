apiVersion: apps/v1
kind: Deployment
metadata:
  name: prime-service
  namespace: prime-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prime-service
  template:
    metadata:
      labels:
        app: prime-service
    spec:
      containers:
      - name: prime-service
        image: prime-service:v1.0.0
        imagePullPolicy: Never  
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "500m"      # 0.5 core
            memory: "128Mi"
          limits:
            cpu: "2000m"     # 2 core
            memory: "512Mi"  
        readinessProbe:
          httpGet:
            path: /prime/2
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10